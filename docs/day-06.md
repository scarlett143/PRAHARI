# Day 6 — Merkle, Quantum Demo, and UI

Completed: domain-separated SHA-256 Merkle batches, inclusion proofs, optional real Polygon contract submission without fake-hash fallbacks, Qiskit Aer QRNG adapter, BB84 protocol simulation/QBER metrics, entropy reporting, experiment logs, and a polished chat/security/proof/quantum UI.

Optional dependencies are separated so the core messenger stays fast to build.
